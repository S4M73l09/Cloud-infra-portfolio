---
import MainLayout from '../layouts/MainLayout.astro';

const toBase = (path: string) =>
  `${import.meta.env.BASE_URL.replace(/\/+$/, '')}/${path.replace(/^\/+/, '')}`.replace(/\/{2,}/g, '/');
---

<MainLayout
  title="404 | S4M73l09"
  description="La ruta solicitada no existe en este portafolio."
  language="es"
>
  <section class="hero not-found-hero">
    <p class="whoami-tag">ERROR</p>
    <h1>404</h1>
    <p>La ruta que has intentado abrir no existe o ya no está disponible.</p>
    <div class="whoami-actions">
      <a class="whoami-btn" href={toBase('/')}>Inicio</a>
      <a class="whoami-btn" href={toBase('/en')}>EN Home</a>
    </div>
  </section>
</MainLayout>
